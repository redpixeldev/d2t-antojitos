---
import MenuCategory from '@components/MenuCategory.astro';
import { menuLeft, menuRight, site } from '@data/content';
---

<section
	id='menu'
	class='scroll-mt-20 bg-brand-cream-dark/40 py-20 md:py-28'
	aria-labelledby='menu-title'
>
	<div class='container'>
		<div class='text-center'>
			<p class='script-accent text-brand-red'>Everything we cook</p>
			<h2
				id='menu-title'
				class='mt-1 text-4xl uppercase md:text-6xl'
			>
				The Full Menu
			</h2>
			<a
				href={site.phoneHref}
				class='btn btn-primary mt-6'
			>
				<i
					class='ri-phone-fill'
					aria-hidden='true'></i>
				Call to order · {site.phone}
			</a>
		</div>

		<div class='mt-12 grid items-start gap-6 lg:grid-cols-2'>
			<div class='space-y-6'>
				{menuLeft.map((category) => <MenuCategory category={category} />)}
			</div>
			<div class='space-y-6'>
				{menuRight.map((category) => <MenuCategory category={category} />)}
			</div>
		</div>

		<p class='mt-10 text-center text-sm text-brand-ink/60'>
			Hungry? Call us to place your order — <a
				href={site.phoneHref}
				class='font-semibold text-brand-red underline-offset-2 hover:underline'>{site.phone}</a
			>
		</p>
	</div>
</section>
